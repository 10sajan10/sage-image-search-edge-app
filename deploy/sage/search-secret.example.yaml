# Do not commit a real key. Copy this file outside the repository, replace the
# placeholder with a long random value, and apply the copy.
apiVersion: v1
kind: Secret
metadata:
  name: image-search-secret
  namespace: default
type: Opaque
stringData:
  SEARCH_API_KEY: "REPLACE_WITH_A_LONG_RANDOM_VALUE"
